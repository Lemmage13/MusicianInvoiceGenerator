<UserControl x:Class="MusicianInvoiceGenerator.Views.Controls.InvoicePreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicianInvoiceGenerator.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="3508" d:DesignWidth="2480"
             Background="White">
    <Grid Margin="250">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!--ADDRESS BLOCK - will need to be made into a control - potentially to change based on types of contact info available-->
                <TextBlock Text="NAME" FontSize="100" Margin="20,20,20,20"/>
                <TextBlock Text="Line1" FontSize="60" Margin="12,12,12,12" Grid.Row="1"/>
                <TextBlock Text="Line2" FontSize="60" Margin="12,12,12,12" Grid.Row="2"/>
                <TextBlock Text="Town" FontSize="60" Margin="12,12,12,12" Grid.Row="3"/>
                <TextBlock Text="PostCode" FontSize="60" Margin="12,12,12,12" Grid.Row="4"/>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!--INVOICE DETAILS BLOCK - control to be added, could include option for customer number/id-->
                <TextBlock Text="INVOICE" HorizontalAlignment="Right" FontSize="100" Margin="20,20,20,20"/>
                <TextBlock Text="Invoice No" HorizontalAlignment="Right" FontSize="60" Margin="12,12,12,12" Grid.Row="1"/>
                <TextBlock Text="Date" HorizontalAlignment="Right" FontSize="60" Margin="12,12,12,12" Grid.Row="2"/>
                <TextBlock Text="Due Date" HorizontalAlignment="Right" FontSize="60" Margin="12,12,12,12" Grid.Row="3"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
